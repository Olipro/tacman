-- TacacsPlus Manager log summary --

<% for log in @logs %><%= "#{log.created_at.strftime("%Y-%m-%d %H:%M:%S %Z")} #{@managers[log.owning_manager_id].name}: #{log.level} #{log.username} #{log.message}\n" %><% end %>